import React from 'react'

const ChildProduct = (props) => {
  return (
    <div>
        {/* <h5>id:{props.id}</h5>
        <h5>name:{props.name}</h5>  
        <h5>price:{props.price}</h5>
        <h5>quantity:{props.quantity}</h5> */}
        {/* <button onClick={()=>props.bertambah()}>+</button>
        <button onClick={()=>props.berkurang()}>-</button> */}
    </div>
  )
}

export default ChildProduct